# ===== Mango — fufexan-inspired =====

exec-once=noctalia

# ---------- Monitores ----------
# Mantidos exatamente como estavam. Vamos tratar regras por make/model em uma etapa separada.
monitorrule=name:^HDMI-A-3$,width:2560,height:1440,refresh:60,x:0,y:0,scale:1
monitorrule=name:^eDP-1$,width:1920,height:1080,refresh:60,x:320,y:1440,scale:1

# ---------- Cursor ----------
cursor_theme=Bibata-Modern-Classic
cursor_size=16

# ---------- Teclado / touchpad ----------
xkb_rules_layout=br
repeat_rate=25
repeat_delay=600
tap_to_click=1
disable_while_typing=1
trackpad_natural_scrolling=0

# ---------- Visual: inspirado no fufexan ----------
# Hyprland dele: gaps_in=4, gaps_out=8, border=1, rounding=10.
gappih=4
gappiv=4
gappoh=8
gappov=8
borderpx=1
border_radius=10
no_radius_when_single=0

# One Dark / accent do fufexan.
rootcolor=0x1e2127ff
bordercolor=0x00000088
focuscolor=0x88888888
urgentcolor=0xe06c75ff

# Aproximação dos efeitos do Hyprland dele no Mango.
blur=1
blur_layer=1
blur_optimized=1
blur_params_num_passes=4
blur_params_radius=7
blur_params_noise=0.01
blur_params_brightness=1.0
blur_params_contrast=1.0
blur_params_saturation=1.0

shadows=1
layer_shadows=1
shadow_only_floating=0
shadows_size=10
shadows_blur=30
shadows_position_x=0
shadows_position_y=15
shadowscolor=0x00000055

animations=1
layer_animations=1
focused_opacity=1.0
unfocused_opacity=1.0

# ---------- Sessão ----------
bind=SUPER+SHIFT,r,reload_config
bind=SUPER+SHIFT,e,quit
bind=SUPER,l,spawn,bocchi-lock

# ---------- Apps ----------
bind=SUPER,Return,spawn,foot
bind=SUPER,t,spawn,foot
bind=SUPER,b,spawn,zen-beta
bind=SUPER,e,spawn,waydir

# ---------- Noctalia ----------
bind=SUPER,space,spawn,noctalia msg panel-toggle launcher
bind=SUPER,d,spawn,noctalia msg panel-toggle launcher
bind=SUPER,s,spawn,noctalia msg panel-toggle control-center
bind=SUPER,comma,spawn,noctalia msg settings-toggle

# ---------- Janelas ----------
bind=SUPER,q,killclient,
bind=SUPER,f,togglefullscreen,
bind=SUPER,v,togglefloating,
bind=SUPER,Tab,toggleoverview,

bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
bind=SUPER,Up,focusdir,up
bind=SUPER,Down,focusdir,down

bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down

# ---------- Workspaces (tags) ----------
bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0
bind=SUPER,6,view,6,0
bind=SUPER,7,view,7,0
bind=SUPER,8,view,8,0
bind=SUPER,9,view,9,0

bind=SUPER+SHIFT,1,tag,1,0
bind=SUPER+SHIFT,2,tag,2,0
bind=SUPER+SHIFT,3,tag,3,0
bind=SUPER+SHIFT,4,tag,4,0
bind=SUPER+SHIFT,5,tag,5,0
bind=SUPER+SHIFT,6,tag,6,0
bind=SUPER+SHIFT,7,tag,7,0
bind=SUPER+SHIFT,8,tag,8,0
bind=SUPER+SHIFT,9,tag,9,0

# ---------- Screenshot ----------
bind=NONE,Print,spawn_shell,grim -g "$(slurp)" - | wl-copy

# ---------- Mídia ----------
bind=NONE,XF86AudioPlay,spawn,playerctl play-pause
bind=NONE,XF86AudioPrev,spawn,playerctl previous
bind=NONE,XF86AudioNext,spawn,playerctl next
bind=NONE,XF86AudioRaiseVolume,spawn,noctalia msg volume-up
bind=NONE,XF86AudioLowerVolume,spawn,noctalia msg volume-down
bind=NONE,XF86AudioMute,spawn,noctalia msg volume-mute
bind=NONE,XF86MonBrightnessUp,spawn,noctalia msg brightness-up
bind=NONE,XF86MonBrightnessDown,spawn,noctalia msg brightness-down

# ---------- Mouse ----------
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize
